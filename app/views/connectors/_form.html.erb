<%= form_for(@connector) do |f| %>
  <% if @connector.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@connector.errors.count, "error") %> prohibited this connector from being saved:</h2>

      <ul>
      <% @connector.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :channel %><br>
    <%= f.select :channel, options_for_select(@channels, :selected => f.object.channel) %>
  </div>
  <div class="field">
    <%= f.label :mug_number %><br>
    <%= f.select :mug_number, options_for_select(@numbers, :selected => f.object.mug_number) %>
  </div>
  <div class="field">
    <%= f.label :user_number %><br>
    <%= f.text_field :user_number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
